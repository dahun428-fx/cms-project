<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.co.cms.dao.EmployeeDao">
	<select id="getRoleArrByEmployeeId" parameterType="string" resultType="UserRole">
		SELECT 
			id 				as "userId",
			role_name 		as "roleName"
		FROM 
			USER_ROLE
		WHERE
			id = #{value}
	</select>
	<select id="getEmployeeById" parameterType="string" resultType="Employee">
		<!-- 
			EMP_NO number NOT NULL,
			EMP_ID varchar2(45) NOT NULL UNIQUE,
			EMP_PWD varchar2(100) NOT NULL,
			EMP_NAME varchar2(45) NOT NULL,
			BIRTH date NOT NULL,
			GENDER varchar2(45) NOT NULL,
			PHONE varchar2(45),
			EMAIL varchar2(45),
			ADDRESS varchar2(45),
			JOIN_DATE timestamp,
			WITHDRAW_DATE timestamp,
			EMP_STATUS varchar2(45),
			DEPT_CODE number NOT NULL,
		 -->
		 SELECT
		 	emp_no 				as "no",
		 	emp_id 				as "id",
		 	emp_pwd 			as "pwd",
		 	emp_name 			as "name",
		 	birth 				as "birthDate",
		 	gender,
		 	phone,
		 	email,
		 	address,
		 	join_date 			as "joinDate",
		 	withdraw_date 		as "withDrawDate",
		 	emp_status 			as "status",
		 	dept_code 			as "departmentCode"
		 FROM
		 	employee
		 WHERE
		 	emp_id = #{value}
	
	</select>

</mapper>